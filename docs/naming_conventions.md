# Naming Conventions

- `raw`: source-aligned records from CityPulse ETL
- `staging`: standardized fields and derived metrics
- `warehouse`: dimensions and fact tables
- `marts`: reporting views

Dimension tables use `dim_`; fact tables use `fact_`.
